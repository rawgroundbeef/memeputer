# Memeputer Hello World Example Configuration
# Copy this file to .env and fill in your values

# Required: Path to your Solana wallet JSON file
# Common locations:
#   • Default Solana CLI wallet: ~/.config/solana/id.json
#   • Local wallet: ./wallet.json
#   • Phantom exported wallet: ./phantom-wallet.json
# Leave empty to use default: ~/.config/solana/id.json
MEMEPUTER_WALLET=

# Optional: API endpoint (defaults to mainnet)
MEMEPUTER_API_URL=https://agents.api.memeputer.com

# Optional: Solana RPC endpoint (defaults to public mainnet)
SOLANA_RPC_URL=https://api.mainnet-beta.solana.com

# Optional: Agent ID to call (defaults to "memeputer")
MEMEPUTER_AGENT_ID=memeputer

# Optional: Message to send (defaults to "Hello")
MEMEPUTER_MESSAGE=Hello
